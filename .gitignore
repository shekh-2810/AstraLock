# ── CMake build artifacts ─────────────────────────────────────
build/
build-pam/
build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
_CPack_Packages/
*.cmake
!CMakeLists.txt

# ── Compiled outputs ──────────────────────────────────────────
*.o
*.a
*.so
*.so.*
*.out
facelockd
pam_facelock.so

# ── Generated headers ─────────────────────────────────────────
# version.h is generated by cmake from version.h.in — don't commit the output
generated/
daemon/include/facelock/version.h

# ── ONNX models — downloaded at install time, not stored in repo ──
*.onnx
*.onnx.bak

# ── Face data / runtime state — never commit biometric data ──
/var/
*.bin
*.emb

# ── Temp / IPC artifacts ──────────────────────────────────────
_facelock_ipc.py
*.sock
/run/

# ── Editor / IDE ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Packaging artifacts ───────────────────────────────────────
*.deb
*.rpm
*.tar.gz
*.tgz
*.zip

# ── Logs / test output ────────────────────────────────────────
*.log
test_output/

# ── Notes (local scratch, not for repo) ──────────────────────
notes*.md
